# View users
sudo docker-compose exec db psql -U user -d transactiwar -c "SELECT * FROM users;"

# View logs
sudo docker-compose exec db psql -U user -d transactiwar -c "SELECT * FROM logs;"

# View transactions
sudo docker-compose exec db psql -U user -d transactiwar -c "SELECT * FROM transactions;"

# Interactive shell
sudo docker-compose exec db psql -U user -d transactiwar


# 1. Stop containers and remove the old MySQL data volume
cd /home/ankit/Trial/TransactiWar
sudo docker-compose down --remove-orphans -v

# 2. Remove any stale containers
sudo docker rm -f $(sudo docker ps -aq --filter "name=transactiwar") 2>/dev/null

# 3. Rebuild and start with PostgreSQL
sudo DOCKER_BUILDKIT=0 docker-compose up -d --build
